<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<configuration xmlns="urn:activemq" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="urn:activemq /schema/artemis-configuration.xsd">
  <!-- Basic stuff -->
  <core xmlns="urn:activemq:core">
    <name>721274fcb7bd</name>
    <persistence-enabled>true</persistence-enabled>
    <security-enabled>false</security-enabled>
    <acceptors>
      <!-- AMQP 1.0 -->
      <acceptor name="amqp">tcp://0.0.0.0:5672?protocols=AMQP</acceptor>
      <!-- OpenWire -->
      <acceptor name="artemis">tcp://0.0.0.0:61616?protocols=CORE,OPENWIRE</acceptor>
      <!-- Web console needs this -->
      <acceptor name="web">tcp://0.0.0.0:5445?protocols=CORE</acceptor>
    </acceptors>
    <security-settings>
      <security-setting match="#">
        <permission type="createAddress" roles="amq"/>
        <permission type="createDurableQueue" roles="amq"/>
        <permission type="deleteDurableQueue" roles="amq"/>
        <permission type="createNonDurableQueue" roles="amq"/>
        <permission type="deleteNonDurableQueue" roles="amq"/>
        <permission type="consume" roles="amq"/>
        <permission type="send" roles="amq"/>
      </security-setting>
    </security-settings>
    <!-- Explicit address + queue definition -->
    <addresses>
      <address name="mq-stress-tester">
        <anycast>
          <queue name="mq-stress-tester"/>
        </anycast>
      </address>
    </addresses>
  </core>
</configuration>
